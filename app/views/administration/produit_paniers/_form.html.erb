<%= simple_form_for([:administration, User.find(params[:user_id]),Panier.find(params[:panier_id]),@produit_panier]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
   	<%= f.association :stock, :collection => Stock.where(:user_id => params[:user_id]).map{ |s| [s.titre,s.id]} %>
    <%= f.input :quantite %> <span> KG (à changer pour le mettre en JS selon le produit du stock selectionne)</span>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
