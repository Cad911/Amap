<div class="page-header">
   	<h1>Listes des  produits autorises</h1>
</div>



<table class="table table-striped">
  <tr>
    <th>Id</th>
    <th>Dirigé par</th>
    <th>Categorie</th>
    <th>Titre</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @produit_autorises.each do |produit_autorise| %>
  <tr>
    <td><%= produit_autorise.id %></td>
    <td><%= produit_autorise.user.nom %></td>
    <td><%= produit_autorise.categorie.nom %></td>
    <td><%= produit_autorise.titre %></td>
    <td><%= produit_autorise.description %></td>
    <td><%= link_to 'Plus d\'info', administration_user_produit_autorise_path(params[:user_id],produit_autorise) ,:class => "btn btn-primary"%></td>
    <td><%= link_to 'Modifier', edit_administration_user_produit_autorise_path(params[:user_id],produit_autorise) ,:class => "btn" %></td>
    <td><%= link_to 'Supprimer', administration_user_produit_autorise_path(params[:user_id],produit_autorise), confirm: 'Are you sure?', method: :delete ,:class => "btn btn-warning"%></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Nouveau Produit autorise', new_administration_user_produit_autorise_path %>
