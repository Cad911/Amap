<div class="page-header">
   	<h1>Liste panier autorises</h1>
</div>




<table class="table table-striped">
  <tr>
    <th>Id</th>
    <th> Dirigé par </th>
    <th>Categorie</th>
    <th>Titre</th>
    <th>Description</th>
    <th>Prix panier ht</th>
    <th>Prix panier ttc</th>
    <th>Deleted</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @panier_autorises.each do |panier_autorise| %>
  <tr>
    <td><%= panier_autorise.id %></td>
    <td><%= panier_autorise.user.nom %></td>
    <td><%= panier_autorise.categorie.nom %></td>
    <td><%= panier_autorise.titre %></td>
    <td><%= panier_autorise.description %></td>
    <td><%= panier_autorise.prix_panier_ht %></td>
    <td><%= panier_autorise.prix_panier_ttc %></td>
    <td><%= link_to 'Plus d\'info', administration_user_panier_autorise_path(params[:user_id],panier_autorise),:class => "btn btn-primary" %></td>
    <td><%= link_to 'Modifier', edit_administration_user_panier_autorise_path(params[:user_id],panier_autorise),:class => "btn"%> </td>
    <td><%= link_to 'Supprimer', administration_user_panier_autorise_path(params[:user_id],panier_autorise), confirm: 'Are you sure?', method: :delete,:class => "btn btn-warning"%> </td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Nouveau Panier autorise', new_administration_user_panier_autorise_path %>
