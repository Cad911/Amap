// Generated by CoffeeScript 1.2.1-pre
(function() {
  var Lightbox;

  Lightbox = (function() {

    Lightbox.name = 'Lightbox';

    function Lightbox(id_lightbox, div_in_lightbox) {
      this.id_lightbox = id_lightbox;
      this.div_in_lightbox = div_in_lightbox != null ? div_in_lightbox : false;
      console.log('lalaal');
      this.event();
    }

    Lightbox.prototype.event = function() {
      var that;
      that = this;
      return $('.annuler,.close_lightbox').bind('click', function() {
        that.hide();
        if (that.div_in_lightbox !== false) return that.hide_sous_div();
      });
    };

    Lightbox.prototype.link_top = function(data) {
      var a_deconnexion, a_infos;
      a_infos = $(document.createElement('a'));
      a_infos.attr('href', data['link']['mes_infos']);
      a_infos.text('Mes infos');
      a_deconnexion = $(document.createElement('a'));
      a_deconnexion.attr('href', data['link']['deconnexion']);
      a_deconnexion.text('deconnexion');
      $('.login>span.sign_in').html(a_infos);
      return $('.login>span.sign_up').html(a_deconnexion);
    };

    Lightbox.prototype.title_header = function(texte) {
      return $(this.id_lightbox + '>.lightbox>.header>.title').text(texte);
    };

    Lightbox.prototype.title_content = function(texte) {
      return $(this.id_lightbox + '>.lightbox>.content>.title').text(texte);
    };

    Lightbox.prototype.text_content = function(texte) {
      return $(this.id_lightbox + '>.lightbox>.content>p').text(texte);
    };

    Lightbox.prototype.hide = function() {
      return $(this.id_lightbox).css('display', 'none');
    };

    Lightbox.prototype.show = function() {
      return $(this.id_lightbox).css('display', 'block');
    };

    Lightbox.prototype.hide_sous_div = function() {
      return $(this.div_in_lightbox).css('display', 'none');
    };

    Lightbox.prototype.show_sous_div = function() {
      return $(this.div_in_lightbox).css('display', 'block');
    };

    return Lightbox;

  })();

  exports.Lightbox = Lightbox;

}).call(this);
